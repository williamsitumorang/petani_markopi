import 'package:flutter/material.dart';

class KomunitasPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Komunitas Page'),
      ),
      body: Center(
        child: Text('Halaman Komunitas'),
      ),
    );
  }
}
