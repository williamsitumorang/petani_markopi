import 'package:flutter/material.dart';

class PanenPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Panen Page'),
      ),
      body: Center(
        child: Text('Halaman Panen'),
      ),
    );
  }
}
