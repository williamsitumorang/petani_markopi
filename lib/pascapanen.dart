import 'package:flutter/material.dart';

class PascaPanenPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Pasca Panen Page'),
      ),
      body: Center(
        child: Text('Halaman Pasca Panen'),
      ),
    );
  }
}
